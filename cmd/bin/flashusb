#!/usr/bin/env bash
#> Flash a bootable ISO image to a USB device.
#> Usage: flashusb <iso_file> <device>
#> Example: flashusb ubuntu.iso /dev/sdb

set -euo pipefail
IFS=$'
	'

usage() {
    echo "Usage: flashusb <iso_file>"
}

if [ $# -ne 1 ]; then
    usage
    exit 1
fi

caligula burn "$1"
