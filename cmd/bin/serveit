#!/usr/bin/env bash
#> Serve a directory over HTTP

set -euo pipefail
IFS=$'
	'

usage() {
    echo "Usage: serveit <port>"
}

if [ $# -ne 1 ]; then
    usage
    exit 1
fi
# check that the arugment is a number
if ! [[ "$1" =~ ^[0-9]+$ ]]; then
    usage
    exit 1
fi

python -m http.server "$1"
