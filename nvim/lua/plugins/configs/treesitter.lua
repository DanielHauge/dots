local options = {
  ensure_installed = {},
  -- Set parser install dir
  highlight = {
    enable = true,
    use_languagetree = true,
  },
  sync_install = false,
  indent = { enable = true },
}

return options
